stream->reduce

baeldung.com/java-stream-reduce

- produces one single result from a sequence of elements
- params:
    - identity: initial val
    - accumulator: func/lambda with two params (partial result and next val)

EXAMPLE
List<Integer> stack = Arrays.asList(3, 2, 1);
int res = stack.stream().skip(1).reduce(stack.get(0), (a, b) -> a - b); // 0

List.of("here", "i", "am").stream().map(String::length).reduce(0, Integer::sum);
$1 ==> 7

