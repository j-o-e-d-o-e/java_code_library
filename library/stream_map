Stream->Map



- groupingBy() returns Map.Entry (by default, keys are lists of grouped entities)
- Function.identity() returns initial value

EXAMPLE
(1)
Map<String, Long> occurrences = Stream.of("j", "o", "e", "d", "o", "e")
        .collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));
System.out.println(occurrences);
