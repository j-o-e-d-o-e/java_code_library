Set->LinkedHashSet

data structure

- ordered subclass of HashSet<T>
    - maintains insertion order (like List)
- init (additionally to hashset)
    - LinkedHashSet(int initialCapacity, float loadFactor)
