string->builder

built-in class

- mutable (unlike String)
- methods
    - append(str) appends str to this
    - delete(start, end) deletes chars from start to end (exclusive)
    - insert(index, str) inserts str at index of this

EXAMPLE
StringBuilder mutant = new StringBuilder("some");
mutant.append(" ").append("stuff"); // some stuff
mutant.delete(5,7);                 // some uff
mutant.insert(5, "p");              // some puff
mutant.reverse();                   // ffup emos
