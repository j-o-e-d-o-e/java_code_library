string->builder



- mutable object (unlike string)
- 'append(str)' appends a string
- 'delete(start, end)' deletes characters from start to end (exclusive)
- 'insert(i, str)' inserts string at specified index

EXAMPLE
StringBuilder mutant = new StringBuilder("some");
mutant.append(" ").append("stuff"); // some stuff
mutant.delete(5,7);                 // some uff
mutant.insert(5, "p");              // some puff
mutant.reverse();                   // ffup emos
